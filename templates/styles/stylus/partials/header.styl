.page-header
  clear()
  box-shadow(0 2px 2px rgba(#000, .25))
  position fixed
  top 0
  left 0
  z-index 10
  width 100%
  height 64px
  line-height 64px
  background $primary-color
  color #fff

  .logo
    float left
    font-size 32px
    font-family Roboto
    font-weight: 900
    text-shadow 1px 1px 2px rgba(#000, .6),;